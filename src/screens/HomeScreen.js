import React from "react";
import { Text, StyleSheet } from "react-native";

const HomeScreen = () => {
  return <Text style={styles.text}>HomeScreen Its me BaOai deps trasieu dep taii</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 65,
    color: "blue",
  },
});

export default HomeScreen;
